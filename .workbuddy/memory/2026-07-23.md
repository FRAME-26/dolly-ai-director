# 2026-07-23

- 读取并分析了 `research assets/Dolly-AdventureX-Tracks-and-Themes.pdf`（Dolly x AdventureX 2026 赛道策略，作者 Jeboy/Nero/Ceaser Zhao）。
  - 黑客松时间线：Stage1 = 7/25 14:00（锁定队伍/赛道/主题/选题）；Stage2 = 7/26 01:00（完整交付，核心开发 7/24 截止）。每人最多 6 个赛道。
  - 6 赛道方案：Dimensional(A+ 主战场)、bilibili(A- 最大池)、superun.ai(B+ 叙事免费)、AI Native Youth(B+)、Qoder(B 过程彩票)、Photon(C+ 产品协同附加)。
  - 主题选定：Kaleidoscope（"Many lenses, one story"）；备选 Reverse。
  - 决策闸门：7/23 晚（Photon 去留）、7/24 末（锁定方案+主题）、表单开放时提交 Stage1。
  - 与后端相关：Photon 赛道 = 通过 Spectrum API 外发"成片已好"消息（client/backend 的集成点，可在 models.py 契约里预留 message 事件）。

- 集成前端 `research assets/Dolly_front-main` → 仓库 `frontend/`（React19 + Vite + TS + Tailwind + XState，按 Figma 实现 Dolly V2 网页端）。
  - 前端已内置后端契约：`WS /ws/voice`、`POST /export/otio`、`POST /export/publish`，经 `VITE_DOLLY_API` 指后端；未配置则跑 mock demo。
  - 已写 `backend/API_CONTRACT.md` 供 Ceaser 实现对应端点（含 CORS + `/health`）；`frontend/.env` 默认指向 `http://localhost:8000`。
  - 部署：前端 Vercel（vercel.json 已带），后端 systemd（已有）；根 README 已加 Frontend 段，.gitignore 加了 node_modules/dist。

- 部署方案升级为 **Vercel(前端) + Railway(后端)** 的托管组合（替代/并存 systemd 自托管）。
  - 新增 `backend/Procfile`（`web: uvicorn app.main:app --host 0.0.0.0 --port $PORT`）、`backend/railway.toml`（Nixpacks + healthcheck `/health` + on_failure 重启）。Railway 须把 Root Directory 设为 `backend/`。
  - 前端 `frontend/vercel.json` 补上 `buildCommand: pnpm build` + `outputDirectory: dist`（原为纯 SPA rewrite）；加 `frontend/.nvmrc`(22) 锁 Node 版本（Vite 8 需 Node ≥20.19/22.12）。Vercel Root Directory 设 `frontend/`，env `VITE_DOLLY_API` = Railway 域名。
  - 写 `deploy/VERCEL_RAILWAY.md` 全套步骤 + 联调清单 + 与 systemd 方案的二选一关系；README 加对应段落与结构树。
  - **关键坑**：`VITE_DOLLY_API` 是 Vite build-time 内联，改后端地址必须重部署前端；Railway 注入真实 env var（`$PORT` + dashboard 变量），后端别假设有 `.env`；Railway 原生支持 WS。

- 接收官方架构 `Dolly_AI_Engine_Architecture_ZH.md`（July 23 Final Lock 对齐）。核心：**AI 引擎 ≠ 后端**，且云端不在实时链路。
  - 实时链路全在局域网：OBS 笔记本跑 **Nero 后端**（帧中继 + 安全裁决 hold/cooldown/margin/health + OBS 桥 + events.jsonl + 本地 Director Console）；4060 笔记本跑 **Ceaser 的 AI 引擎**（拉帧→YOLO→四子分打分→director.decision），作为 **WS 客户端**连后端 `WS /ws/v1/engine`，只提议不执行。
  - Railway 仅赛后归档（POST /archive/session + 摘要），Vercel 仅展示 + 只读 Session Replay；二者**非实时**，断网照跑。
  - 新增 `dolly-engine/`（空文件，按 §7：main/config/fetcher/perception/heuristic/scoring/decision/ws_client/replay + requirements.txt 起步）。
  - 重写 README（加「AI 引擎≠后端」铁律表、结构树、本地实时部署段），重写 `deploy/VERCEL_RAILWAY.md` 为"非实时云端"范围。
  - **待团队确认的三大冲突**：(1) `backend/app/decision_engine.py` 应迁到 dolly-engine（scoring/decision），后端改 `safety.py`；(2) `client/` 的 cv2 直开摄像头/YOLO/OBS 控制与架构冲突，应并入 dolly-engine(感知) 与 backend(OBS桥)；(3) `frontend/` 设计稿用 `/ws/voice`、`/export/*`，但锁定合同是 `/ws/v1/engine` + `/api/v1/frames/`，需对齐 v1.0。
